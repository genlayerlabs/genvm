From 0000000000000000000000000000000000000000 Mon Sep 17 00:00:00 2001
From: kp2pml30 <kp2pml30@gmail.com>
Date: Wed, 28 Jan 2026 22:31:50 +0900
Subject: [PATCH] chore: expose bounds check

---
 crates/wiggle/src/lib.rs | 5 +++++
 1 file changed, 5 insertions(+)

diff --git a/crates/wiggle/src/lib.rs b/crates/wiggle/src/lib.rs
index a7f4bed3..959807e3 100644
--- a/crates/wiggle/src/lib.rs
+++ b/crates/wiggle/src/lib.rs
@@ -275,6 +275,11 @@ impl<'a> GuestMemory<'a> {
         Ok(mid)
     }
 
+    pub fn bounds_check(&self, ptr: GuestPtr<[u8]>) -> Result<(), GuestError> {
+        let _range = self.validate_range::<u8>(ptr.pointer.0, ptr.pointer.1)?;
+        Ok(())
+    }
+
     fn validate_range<T>(&self, offset: u32, len: u32) -> Result<Range<usize>, GuestError>
     where
         T: GuestTypeTransparent,
